{
  "name": "quadro-api",
  "version": "1.0.0",
  "description": "api for quadro credit",
  "main": "server.js",
  "author": "Obi Fortune",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "eslint --cache --fix .",
    "format": "prettier --single-quote --check ."
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.15"
  }
}
